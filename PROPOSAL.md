# hive 절감 제안서 — 시간제(구독) 크레딧을 아끼는 기술

작성: 2026-09-22 · 대상: Freebuff 멀티탭 에이전트 워크플로우
현재 실측: 탭 6개 합계 약 **430만 토큰(추정)** 컨텍스트 적체 — 이대로면 5시간 윈도우를
탭 몇 개가 혼자 태운다. 아래는 리서치 기반 절감 전략 6가지를 hive 구현 난이도순으로 정리.

---

## 0. 왜 지금 문제인가 (구조적 원인)

시간제 요금의 실체 (Claude Code 기준, 업계 공통 패턴):
- **5시간 롤링 윈도우 + 주간 한도**가 토큰 소비량으로 과금. "시간"이 아니라 "토큰"제.
- 멀티탭 = 같은 윈도우를 탭 수만큼 나눠 쓰는 것. 워커 1개가 긴 컨텍스트를 유지하면
  그 윈도우가 통째로 줄어든다.
- 에이전트 세션의 비용 곡선은 **지수적**: 컨텍스트가 쌓일수록 매 턴 전송량이 늘고,
  tool 호출이 많은 턴일수록 출력·재추론 비용이 커진다.

즉 절감 레버는 3개뿐: **① 컨텍스트를 줄인다 ② 턴 수를 줄인다 ③ 같은 토큰을 재활용한다.**

---

## 1. 배치 임무 (Batch Missions) — 턴 수 절감 [난이도: 낮음, 효과: 큼]

**원리**: 워커를 깨우는 횟수 자체가 과금 단위다. 깨우기 1회 = 시스템 프롬프트 재처리 +
컨텍스트 전체 재전송. 임무 5개를 5번 나눠 보내면 5배, 한 번에 보내면 1배.

- 근거: 멀티에이전트 오케스트레이션 패턴 연구들(2026)이 공통으로 권장하는
  **fan-out/fan-in + 배치 큐** 패턴. Azure Architecture Center의 오케스트레이션 가이드도
  "작업을 가능한 한 큰 배치로 묶어 에이전트 깨우기 횟수를 최소화" 지향.
- **hive 반영**: 이미 `dispatch` 가 인박스 append 방식으로 구현됨. 추가할 것:
  - `hive dispatch --file plan.md` — 파일 통째로 임무화
  - 인박스에 지시 누적 후 "곧 보낼게" 표시만 하고 watch가 주기적으로 몰아서 전송
- **예상 효과**: 깨우기 횟수 60~80% 감소.

## 2. 컨텍스트 갱신 스케줄러 (Context Rotation) — 컨텍스트 절감 [난이도: 중간, 효과: 최대]

**원리**: 컨텍스트가 길어진 탭은 **닫고 새 탭**을 여는 게 최종적으로 가장 싸다.
새 세션 = 컨텍스트 0. 긴 세션의 "요약 + 현재 상태"만 새 탭에 심어주면 됨.

- 근거: **TokenPilot (arXiv 2606.17016, 2026)** — 장기 에이전트 세션의 비용의 대부분이
  "컨텍스트 적체 + 캐시 미스"에서 나옴을 실증. 저자들은 61~87% 비용 절감을 달성.
  핵심 통찰: **텍스트를 줄이는 것보다 "언제 리셋할지"가 더 중요하다.**
- **hive 반영** (다음 버전 후보): `hive rotate "<탭>"`
  1. 탭 마지막 어시스턴트 메시지에서 상태 요약 추출
  2. 새 탭 생성 (`POST /api/threads` 확인됨) + 요약 심기
  3. 기존 탭 close → 윈도우 즉시 회복
- **트리거 규칙**: `hive stats` 에서 추정 토큰이 임계(예: 300k) 넘으면 watch가 자동 제안.
- **예상 효과**: 장기 워커 1개당 5시간 윈도우 사용률 50~70% 감소.

## 3. 캐시 친화적 프롬프트 (Cache-Aligned Prompting) — 재전송 절감 [난이도: 낮음]

**원리**: 프로바이더들은 동일 프롬프트 **접두사**에 캐시 할인(비용 ~25% 수준까지 할인)을
준다. 접두사가 한 글자라도 바뀌면 캐시가 깨져 전체 재전송.

- 근거: prompt caching 가이드들(2026) — "가장 큰 절감, 가장 낮은 리스크"로 꼽힘.
  시스템 프롬프트·도구 정의를 **매번 동일한 순서·동일한 내용**으로 유지하는 게 핵심.
- **hive 반영**:
  - `CONTINUE_TEXT`/`INBOX_PING` 문구를 **상수로 고정** (이미 그렇게 함 — 변경 금지 규칙)
  - dispatch 임무문도 "변하는 부분(실제 지시)"을 항상 **메시지 끝에** 두고, 앞머리
    (규약 참조문 등)는 절대 수정하지 않는 템플릿 강제
- **예상 효과**: 캐시 히트율에 따라 실효 입력 비용 20~50% 감소.

## 4. 시맨틱 디듀프 (Semantic Dedup) — 중복 작업 제거 [난이도: 중간]

**원리**: 여러 워커가 같은 파일·같은 질문을 반복 조사하면 토큰이 그대로 2배·3배.
작업 배분 시 **파일 소유권**을 명시해 중복을 원천 차단.

- 근거: multi-agent orchestration 문헌(2026)의 공통 함정 — "orchestrator가 서로 다른
  워커에게 겹치는 하위 작업을 주면 전체 비용이 배로 늘어난다".
- **hive 반영**: `dispatch` 가 임무문을 워커별로 분할할 때 파일 경로 목록을 파싱해
  **같은 경로를 두 워커에 주지 않는 검증** 추가. 충돌 시 HEAD 탭에 경고.
- **예상 효과**: 병렬 검수류 작업에서 20~40% 감소.

## 5. 적응형 감시 (Adaptive Watch) — 무의미한 깨우기 제거 [난이도: 낮음]

**원리**: 감시 주기 20초는 넉넉하지만, 야간처럼 아무 일도 없을 때도 계속 도는 건
낭비(크레딧은 아니지만 기회비용·로그). 반대로 바쁠 때는 더 촘촘히.

- 근거: LLM 운영 비용 가이드(2026)의 " enforced budgets" 패턴 — 사전에 예산을 걸어두고
  초과 시 자동 완화 동작.
- **hive 반영**:
  - 시간대별 주기: 09~24시 20초 / 00~09시 120초 (야간 완화)
  - `hive watch --budget N` — 1시간당 최대 컨티뉴 전송 횟수 상한, 초과 시 대기만
- **예상 효과**: 무의미한 재시도 80% 감소 (특히 야간).

## 6. 모델 라우팅 (Model Routing) — 같은 일 싼 모델에게 [난이도: 높음, 장기]

**원리**: 조사·요약·문서 검수류는 저가 모델로, 설계·난이도 높은 코딩만 고성능 모델로.

- 근거: token-budget-aware reasoning 문헌(2026) — 태스크별 라우팅만으로 40~70% 절감 보고.
- **hive 반영**: Freebuff API에 모델 지정 필드(`model`, `harnessId`)가 스냅샷에 보임.
  `hive new --model <name>` 로 탭 생성 시 모델 지정 → 저가 워커 풀 운영.
  (Freebuff 요금제가 모델별 차등 과금인지 확인 필요 — 확인 후 도입)
- **예상 효과**: 워크로드 믹스에 따라 30~60%.

---

## 도입 로드맵 (권장 순서)

| 단계 | 기능 | 난이도 | 예상 절감 |
|---|---|---|---|
| v0.3 | 시간대별 적응형 감시 + budget 옵션 | 낮음 | 무의미 깨우기 80%↓ |
| v0.3 | cache-aligned 상수 규칙 문서화 + 템플릿 강제 | 낮음 | 입력 20~50%↓ |
| v0.4 | dispatch --file + 소유권 충돌 검증 | 중간 | 중복 20~40%↓ |
| v0.4 | stats 임계 알림 (컨텍스트 갱신 제안) | 중간 | 윈도우 회복 |
| v0.5 | rotate 자동화 (요약→새 탭→구탭 close) | 높음 | 장기 탭 50~70%↓ |
| v1.0 | 모델 라우팅 (요금제 확인 후) | 높음 | 30~60%↓ |

## 참고 자료

- TokenPilot: Cache-Efficient Context Management for LLM Agents (arXiv:2606.17016, 2026)
- The Orchestration of Multi-Agent Systems: Architectures (arXiv:2601.13671, 2026)
- Azure Architecture Center — AI Agent Design Patterns (2026)
- Claude Code — Manage costs effectively (docs, 2026)
- Token-Budget-Aware LLM Reasoning (Redis blog, 2026)
- awesome-llm-token-optimization (GitHub, 2026)
